# Pre-launch teasers (2026-09-22 → 2026-10-12)

No signup link exists yet (calendar.md's dependency #1), so these three
posts point at the one channel that's already real and already a
receipt: the public GitHub repo. No ask, no CTA beyond "go look" — the
job here is planting the premise before launch day has to carry all of
it at once.

## Teaser 1 — week of 2026-09-22: "the premise"

X:

```
Building a company where every decision is public.

Not a mission statement — literally: every call this project makes
gets logged as a dated, timestamped entry in the repo. Including the
ones that didn't work. No cleanup after the fact.

Been running like this since day one. Repo's open if you're curious
what that actually looks like: github.com/alexandrapaiz/alexandria
```

## Teaser 2 — week of 2026-09-29: "the receipts"

X, with a screenshot of the harness-engineering SKILL.md frontmatter
(the provenance block: claims, papers, validated date) attached:

```
Every Claude skill I've seen ships as instructions with no way to
check if it's any good.

This one ships with its receipts: the 12 claims it's built on, the 5
papers behind them, and a dated test of whether loading it actually
changed what the model recommended (it did — logged, not asserted).

skills/harness-engineering in the repo:
github.com/alexandrapaiz/alexandria
```

## Teaser 3 — week of 2026-10-06: "the countdown"

X + LinkedIn (LinkedIn: same content, drop the emoji/fragment style,
write full sentences per linkedin.md's register):

```
One week out from launching alexandria: a free weekly digest on AI
research (what's trailblazing, what's held up, what's been left
behind — each with its sources), and a paid layer ($20/mo) of Claude
skills built the way the last two posts showed — evidence attached,
not just instructions.

Oct 13. Most of the org running it between now and then is autonomous
agents; I'm the one deciding what actually ships.

github.com/alexandrapaiz/alexandria
```

**Owner's call on cadence:** these can run on X alone, or X+LinkedIn
for teaser 3 only, per the calendar. None require the site to be fixed
first — that's precisely why they're sequenced before launch day
instead of after.
