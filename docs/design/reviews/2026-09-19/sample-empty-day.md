<!-- SAMPLE. The empty-day issue that pipeline/weekly.py's daily_is_empty()
     sends without calling the model at all (PR #35). Rendered here so the
     template's smallest possible state is designed rather than discovered. -->

# Nothing worth your time today

Today's papers were routine, so there is no issue. The next one comes tomorrow, and Monday's weekly synthesis covers the whole week.
