# Editorial review — 2026-09-19

First run of the writer seat (ADR-28). Graded issue: **2026-W37**,
"Richer feedback boosts long-horizon agents [September 7–13, 2026]",
written 2026-09-14 by `openai/gpt-oss-120b` against prompt
`83a0aa3be13c`, read from the digests table. Read cold and whole
before any line was judged.

The owner's dispatch asked for two issues. The daily sample v2 is
committed nowhere, so its shape is reconstructed from canon law 9 in
§2 and used as the yardstick the weekly is measured against. The
weekly is graded against all ten laws in §3.

## 1. The single biggest weakness

**The weekly is a feed of what the pipeline found, ordered by
novelty, and the owner's first principle is impact.** Law 5 says
traction leads. This issue leads with "Trailblazing" and demotes
traction to second, where it becomes ten identical bullets ending in
an unexplained "(3 supports)". Nothing in that section says what
earned any item its slot.

The lead section then spends two of its three slots on the same
paper. Items one and two both cite *T1: Terminal Agent Reinforcement
Learning for Long-Horizon Tasks*, `arxiv.org/abs/2609.11042`. The
whole of Trailblazing rests on two papers out of 3,558 ingested, and
the opening calls them "several teams".

This is the gap the owner has not yet named. Her 2026-09-19 ruling on
traction created the daily's Compounding section, so the daily was
born obeying the relevance law. The weekly never was. It kept the
section order it had before the ruling, and so the product's flagship
issue is the one issue that still reads as a release-date feed.

## 2. The daily's shape, reconstructed (canon law 9)

Law 9 fixes the daily's sections. Taking the law as written, the
daily runs in this order:

1. **Gaining traction**. Work that is gaining acceptance, ordered by
   impact, each slot stating what earned it.
2. **Trailblazing**. The genuinely new, labeled as unproven, so
   freshness is never disguised as importance (taste.md, 2026-09-19).
3. **Left behind**. What lost ground.
4. **Read these yourself**. A chosen reading list, not a dump.
5. **The dual-audience close**. "You read to decide. Your agents load
   to act." (law 10).

*Corrected in run 2.* This list first read "Compounding", "New and
unproven", and a standing "instrument note" section. The owner rejected
all three the same day: the first two as headings that read like a
machine sorting rows, the third as the issue narrating its own method.
Her four section names above are the law, and they were hers before
this seat existed. The reconstruction was wrong to invent names for
sections she had already named, which is the exact mistake the
corrected law 9 now forbids.

Two things follow, and both are findings about the weekly rather than
the daily. First, the daily's order is the relevance law made
structural: traction is section one, novelty is section two, and the
label on section two does the honesty work. The weekly inverts that
order and carries no label. Second, the daily ends on the reader and
the agent. The weekly ends on pipeline counts.

The weekly is not the daily and must not become it. Canon says the
daily may list and the weekly must argue. But "argues" is a stronger
requirement than "lists", not a lighter one, and the weekly currently
does neither.

## 3. The weekly graded, law by law

### Law 1 — Plain sentences, transition words, no stylistic em dashes, no semicolon joins. **FAIL**

The em dash is used as a parenthetical bracket in the opening: "a
**dense verification reward**—counting how many assertions an answer
gets right—produces a **28.5 % relative gain**". Unspaced, on both
sides, exactly the stylistic use the law names. Every source line
carries another: "*paper*: **T1...** — [link]". Both en-dash
lead-ins do the same job: "**Contradicted** – First, the claims newer
evidence says are wrong:".

Counted across the issue: five em dashes, twelve en dashes.

The cause is worth naming, because it is the most useful thing this run
found. **The prompt banned the em dash in roughly thirty of its own
sentences.** "Avoid stylistic em dashes" sat in a bullet that read
"connect them with transition words (so, because, instead, as a
result, however) — but don't reach for the same transition word
twice". A model takes its register from the prose it is shown at least
as much as from the rule it is told, and every instruction block in
that prompt demonstrated the habit the rule forbids. The patch in this
PR rewrites the prompt's own prose to obey the law, leaving the dash
only where it labels a payload field and in one example deliberately
marked as wrong. That is a bigger diff than a rule change and it is
the only version of the fix that can work.

Underneath that sits a typography failure nobody has flagged. The
body contains **87 non-breaking hyphens** (U+2011) in place of
ordinary hyphens, in "Long‑horizon", "reinforcement‑learning",
"sparse‑reward", and every other compound, plus **19 narrow
no-break spaces** (U+202F) producing "28.5 %", "64 %", "122 B".
These are not cosmetic. A reader searching for "long-horizon" in the
archive does not match this text, and the second audience in law 10
loads these bytes. The sentence rules never told the model to write
in plain ASCII punctuation, so it reached for the typesetter's
characters.

### Law 2 — No buzzwords, every line targeted and actionable. **FAIL**

"Read these yourself" is where the targeting dies. Nine entries, and
the lines that justify them say nothing a builder can act on:
"advancing agent design and evaluation pipelines", "exposing design
patterns useful for infrastructure", "a technique that could reshape
RL pipelines". The last one also stacks a hedge onto hype.

The section name "Trailblazing" is itself a claim of excitement the
items have to earn afterward, which is ban-list entry 8.

### Law 3 — Sell the product, never the recipe. **FAIL**

The issue's second line is the recipe: "*The latest in AI research,
read in full and distilled weekly: what's new, what's gaining
acceptance, and what newer evidence has overturned.*" It describes
how the digest is made and then recites its own table of contents.
The issue then closes on the recipe too: "3558 papers ingested • 310
claims distilled • 102 edges drawn".

Both are outside this seat's reach. The masthead is `MASTHEAD` at
`pipeline/weekly.py:311` and the footer is required by the prompt's
own structure block. The masthead is filed for the engineer in
docs/ideas.md, and the prompt change below handles the footer half.

### Law 4 — The headline is a finding, not a greeting. **FAIL**

"Richer feedback boosts long-horizon agents" names a direction, not a
finding. A builder cannot decide from it whether to open the issue,
which is the whole test. The material for a real headline was sitting
in the first item: a dense reward lifting task resolution from 49.4%
to 64.0%.

This costs more than a line. `pipeline/weekly.py:377` uses the H1 as
the **email subject**. The headline is the only thing most readers
ever see, and the prompt's instruction to "lead with the sharpest
concrete detail this week actually has" was advisory enough to lose
to a category label.

### Law 5 — Relevance is impact, traction leads, every ranking says what earned each slot. **FAIL**

The central failure, stated in §1. Three specifics.

Traction does not lead: Trailblazing is section one.

No slot explains itself. Every traction item is the same shape, a
compressed claim plus a parenthetical count: "Puffin-World's native
3D world states remove the need for external offline modules. (3
supports)". Ten of these, the first four all reading "(3 supports)".
The ranking is invisible because the ordering key is invisible.

"(3 supports)" is internal vocabulary. It is the count of incoming
`supports` edges in the claim graph, which is a fact about
alexandria's database, not about the research. No reader can decode
it, and the prompt asked for the opposite: "say what the accumulating
agreement means in one plain sentence."

### Law 6 — Evidence grades in-line. **FAIL, with nothing to grade against**

Not one item in the issue carries an evidence grade. The strongest
number in the issue, the 28.5% relative gain, is stated with no note
that it is the authors' own experiment on their own agent, unreplicated,
reported in the same paper that proposes the method.

The missing grade would have caught a real problem. T1 "resolves 64 %
of tasks on Terminal-Bench 2.1, surpassing GPT-3.5-Turbo and
approaching Claude Opus". A 2026 agent measured against GPT-3.5-Turbo
is measured against a baseline chosen to flatter it. An evidence
grade is the mechanism that makes a weak baseline visible, and the
issue has no such mechanism.

### Law 7 — Honesty notes are product. **FAIL**

Three notes the issue owed the reader and did not write.

Two papers carry the entire Trailblazing section while the footer
advertises 3,558 papers ingested. The opening says "several teams"
when it means two.

"No citation movers were recorded this week." That is stated and
abandoned. A reader cannot tell whether the field was quiet or the
citation instrument did not run, and law 7 exists for exactly that
sentence.

The third Contradicted bullet is not a contradiction. "The same
reference implementation that achieved 82.2 % was later shown to drop
to 12.5 % on memory-intensive tasks" reports a scope limit on a
result the bullet above it already cited. The pipeline drew a
`contradicts` edge and the writer passed it through without judging
it, which is how the same implementation appears twice in one section,
once as the overturner and once as the overturned.

### Law 8 — Links go to the full text. **FAIL, the sharpest one**

Three links in the issue, all of them abstract pages:
`arxiv.org/abs/2609.11042` twice and `arxiv.org/abs/2609.08404`.
Zero `arxiv.org/html/` links.

Then the worse half. **"Read these yourself" contains nine papers and
not one link.** The section whose only job is to send the reader to
the paper gives the reader no way to reach a single one. The prompt's
rule "Every item cites its paper with a markdown link" sits in the
Rules block, while the section's own instruction says "title, link,
one plain line", and the model followed neither. The owner's ruling
was "You didn't show me the paper." Nine times over, it did not.

### Law 9 — Structure, and the weekly must argue. **FAIL**

The weekly lists. Gaining traction is ten bullets, Left behind is six
bullets, Read these yourself is nine bullets. Twenty-five of the
issue's items are bullets and three are prose. Canon takes structure
from Stratechery here: an argument built across sections. Nothing
carries from Trailblazing into Gaining traction, and nothing carries
from either into Left behind. The three sections could be reordered or
published separately with no loss, which is the test for whether an
argument exists.

Measured against the daily's reconstructed shape in §2, the weekly is
missing the honesty label on its new work, the instrument note, and
the close, and it has the first two sections in the wrong order.

### Law 10 — "You read to decide. Your agents load to act." **FAIL**

Absent. The string does not appear. The issue's last line is the
pipeline's own counts, so the one issue this seat can read ends by
talking about itself.

### Ban list

- **Entry 3, symmetric paragraph rhythm.** The opening is four
  paragraphs of near-identical length, three of them opening the same
  way: "A team led by Junyao Yang demonstrated...", "A team led by
  Hongbang Yuan introduced...", "Finally, researchers at the same
  group released...". The prompt warns against this pattern twice and
  the model produced it anyway.
- **Entry 7, summaries that restate the headline.** "Feedback-Enriched
  Environments let agents explore proactively" is followed by "agents
  learn to wander into broader state regions".
- **Entry 4, the hedge stack.** "a technique that could reshape RL
  pipelines", "may replace flat memory-based planning".
- **Entry 12, uniform enthusiasm.** Ten traction bullets at identical
  length and identical shape.
- **Attribution rule broken.** The prompt says institutions first and
  author names only "when institutions are missing". The opening uses
  author names twice.

Four new tells are appended to the ban list this run: typesetter
punctuation, internal pipeline vocabulary at the reader, the complete
dump instead of a judged selection, and one paper wearing several
hats.

## 4. What changed in the generator

Every grade above that failed is answered by a change to
prompts/digest.md in this PR. The mapping:

| Law | Change |
| --- | --- |
| 5 | Traction section moved to first and renamed **Compounding**; each item must state what earned its slot; `(N supports)` must be rendered as plain English; new work renamed **New and unproven** and labeled |
| 8 | `arxiv.org/abs/ID` must be rewritten to `arxiv.org/html/ID` with abs as fallback; every "Read these yourself" entry must carry a link, no exceptions |
| 4 | The title must carry the week's sharpest concrete result, with the decide-from-one-line test and a note that it ships as the email subject |
| 6 | An in-line evidence grade is now required per item, with a short vocabulary, and weak or obsolete baselines must be named |
| 7 | A standing **Instrument note** section, with three named triggers: thin sourcing, a silent stream, and a relation the writer judged wrong |
| 10 | The dual-audience close is now the mandatory last line, after the footer |
| 1 | ASCII punctuation required, the em-dash ban extended to parenthetical pairs and source lines |
| 2, 9 | Read these yourself capped at five with a "why you" line; the weekly must carry one argument named in the opening and picked up by every section |
| 1, all | The prompt's own prose rewritten to obey the punctuation law, because it was teaching the violation by example |
| ban 16 | One paper may not hold two slots without the prose saying both come from the same work |
| prompt | Institution-first attribution moved from advice inside a section to a standing rule |

The section order changed, so I checked that nothing reads it. No
Python, JavaScript, or template in the repo matches on "Trailblazing",
"Gaining traction", "Left behind", or "Read these yourself".
`send_newsletter` touches only the H1 and list spacing, so the rename
is safe.

Two of these also serve the daily the engineer is building, and the
prompt now marks them as cadence-independent: the section order in law
9 and the close in law 10 are written as house rules rather than
weekly-only structure.

## 5. Filed elsewhere

- **docs/ideas.md** — the masthead. It is the recipe in the second
  line of every issue, it is fixed in `pipeline/weekly.py`, and no
  prompt change can reach it. This is the structure-watch rule firing
  on the first run rather than the third, because the fix was never
  available to this seat at all.

## 6. Grade

Zero of ten laws passed.

That number is worth stating plainly rather than softening. The issue
is not badly written line by line. Its sentences are clear, its
numbers are real, and its procedures are genuinely useful. It fails
because the laws describe a product with judgment, honesty, and a
route to the evidence, and the generator was asked for a summary of a
database. The prompt that produced it was written before the ten laws
existed, so this run is the first time the generator has been asked to
answer to them.

---

## 7. Run 2: the prose guide applied, and one thing that is missing

Second run the same day, on the owner's order: merge main, apply the
market seat's newsletter prose guide to the generator on top of her
fine-tuning, verify run 1's patches survived, and read the whole
prompt end to end.

### The guide is a stub

`docs/market/newsletter-prose-guide.md` is on main as ordered, but it
carries a header, a note on why it was commissioned, and the sentence
"Work in progress — populating from primary sources now." There are no
guidance points in it. PR #38's own description says "Research in
progress — marking ready when the guide is complete", and it was
merged before that happened.

So the craft work in this run does not come from the guide. It comes
from the two sources that were available: the canon's References
section, which already names what to take from Morning Brew, Money
Stuff, The Economist, Stratechery and Import AI, and the owner's
rulings in taste.md, which name the four craft problems directly.
That is stated here rather than implied, because when the guide is
populated it should be applied on its own terms, and whoever does that
needs to know this run did not read it.

### What the four craft areas became

**Opening and greeting.** The opening had two jobs and now has three:
greet, orient, interpret. The greeting is one short line, addressed to
the reader, and it has to be true about this particular day. The rule
names its own failure case, because "Welcome to another edition" is
what a model reaches for when told to be warm. Her context-first
invariant is untouched and now governs all three jobs.

**Heading voice.** Her four section names are stated as fixed law with
"Compounding" and "New and unproven" named in the prompt as rejected,
so the next model to read it cannot reinvent them. Heading craft moved
to where it can still be exercised: the item headlines, which state a
finding in plain words and carry no explaining colon.

**Item anatomy.** One spine, at whatever length an item earns: context,
what changed with its institution, the evidence grade in the same
breath, the builder's consequence, the source line. The order never
inverts. It is written as a spine and not a template, because a
template is what the voice section exists to prevent.

**Sign-off.** The issue ended on "papers ingested / claims distilled /
edges drawn". Two of those three are alexandria's internal vocabulary,
which ban-list 14 already forbids printing at the reader, and a row of
counts is a cold way to leave someone. It now ends on a written line
that hands the day back, scale in plain words if scale appears at all,
then the standing close unreworded.

### Run 1's patches, verified

All five survive the merge and still comply. Gaining traction is the
first section in the template. The full-text link rule binds every
section. The title is a finding that ships as the subject line.
Evidence grades are in-line and mandatory on any item with a number.
No meta narration, and that rule is now stronger than it was, for the
reason below.

### What the end-to-end read caught

Six seams, and one of them mattered more than the rest. The
no-meta-section rule was sitting as bare bold text inside the fenced
template, where every other instruction is wrapped in braces. A model
following "exactly this structure" could have printed the paragraph
banning meta-commentary as a section of the newsletter. It is in Rules
now.

The second was the same failure one level down. Left behind opened by
telling the reader why the section was worth reading. Her ruling calls
section intros that justify the section the same disease as ranking
narration, so the prompt was contradicting itself across two adjacent
paragraphs. Both of these were only visible by reading the file as one
document, which is the argument for the step.

### Ban list

Three tells appended: the greeting that carries no information, the
section that defends itself, and headings that name a category instead
of speaking.

### Grade for run 2

Not a graded issue. No new issue has been published since 2026-W37, so
there is nothing to read cold that run 1 did not already read. The
next graded run should be the first issue written by the patched
prompt, and the thing to check first is whether the greeting sounds
like a person or like a model told to sound like a person.

---

## 8. Run 3: the taste gate, and the ruling the generator was overriding

Third run the same day, on the owner's order. Two rulings to encode,
one of them repeated, and a change to how this seat grades.

### The standing procedure, changed (owner's order, run 3)

**Taste compliance is now this seat's FIRST gate.** Before the ten
laws, before the ban list, before any prose judgment: read
docs/voice/taste.md line by line and check the artifact against each
line. Incident 20 is why. A ruling of hers can be recorded in taste.md
within the hour and still not reach the reader, because recording and
enforcing are different acts, and nothing was checking artifacts
against the register before they reached her. This gate is the check.
It runs against the newest issue and against anything reader-shaped
that this seat is about to hand her, the generator's own template
included, because the template is where the issue's headings and title
are really decided.

It is recorded here rather than in prompts/writer-agent.md because the
charter is hers and not this seat's to edit. She asked for it in the
review file, and this section is that.

### The single biggest weakness, run 3

**Her ruling was law in taste.md and the opposite was law in
prompts/digest.md, which this seat owns.** The heading ruling of
2026-09-19 says framework names never print. On the same day, in the
file that writes every issue, this seat had written: "The four section
names are the owner's, and they are fixed. Gaining traction,
Trailblazing, Left behind, Read these yourself. Never rename them."
The template then printed all four as literal `##` headings. Ban-list
entry 19 closed with "The four section names are the owner's and are
fixed", and canon law 9 listed the four names as part of her
fine-tuning. Four files agreed, and all four were wrong.

So the recurrence she flagged with "AGAIN" was not a model ignoring an
instruction. It was a model obeying one. Incident 20's root cause, as
written, is that recording is not enforcing. Run 3 can sharpen it: the
taste ruling landed in the register while three other files kept
instructing the violation, and a ruling that contradicts the generator
loses to the generator every time, because the generator is what runs.
That is the case for the gate, and it is the case for checking the
generator against taste.md and not only the issue.

### The gate, run 1: taste.md line by line against 2026-W37

Still the newest issue. The digests table holds one row, 2026-W37,
written 2026-09-14, so nothing has published since run 1 read it. It
has never been checked against taste.md, and the ten-law grade in §3
is not the same instrument, so the gate runs on it now.

**Standing law**

| Line | Verdict | Evidence |
| --- | --- | --- |
| Not "stale and mechanical and boring" | FAIL | Ten traction bullets, identical shape, four in a row ending "(3 supports)" |
| Register: technical substance, simple language, why-it-matters | FAIL | "WorldReward's chunk-level voting aggregates visual preferences for video-level action scoring." No why, no reader |
| House prose: no em dashes, no semicolon joins, no buzzwords | FAIL | "a **dense verification reward**—counting how many assertions an answer gets right—produces" |

**Rulings, dated 2026-09-19**

| Ruling | Verdict | Evidence |
| --- | --- | --- |
| Traction matters a lot, relevance is impact not release date | FAIL | The new work is section one and traction is section two |
| Fresh work listed honestly as unproven | FAIL | Its heading claims the opposite, and no item carries the honesty |
| The headline is a finding | FAIL | "Richer feedback boosts long-horizon agents" names a direction |
| Evidence grading in-line per item | FAIL | Zero graded items. The 28.5% gain is the authors' own unreplicated result and reads as fact |
| Instrument honesty is product | FAIL | "No citation movers were recorded this week." Stated, then abandoned |
| Links reach the full text | FAIL | Three abstract pages, zero html links, and nine reading-list entries with no link at all |
| The dual-audience close | FAIL | Absent. The last line is "3558 papers ingested • 310 claims distilled • 102 edges drawn" |
| The weekly argues, it does not list | FAIL | Twenty-five bullets, three prose items, and the sections could be reordered with nothing lost |
| No meta-commentary | FAIL | The masthead recites the recipe, and the fell-behind section opens by defending itself: "Older claims are being overturned, and relying on them can lead to brittle systems." |
| Headings are craft, not taxonomy | FAIL | Four taxonomy headings, printed |
| Context-first, nothing given with zero context | SPLIT | The opening obeys it: "Long-horizon reinforcement-learning agents have struggled with sparse rewards and unstable training". Every section and every bullet under it opens cold: "Puffin-World's native 3D world states remove the need for external offline modules." |
| The date does not belong in the title | FAIL | "# Richer feedback boosts long-horizon agents [September 7-13, 2026]" |
| Section headers: same framework, never the same verbatim words | FAIL | Same four words an issue would use every week |
| Length follows the news | FAIL | Two papers of real new work, ten traction bullets and nine reading-list entries. The length describes the queries, not the week |
| Framework names never print, second occurrence | FAIL | The two she named, printed |

Fifteen fail, one splits, none pass. The issue predates every ruling
except the first, so this is a measurement of the distance to cover and
not a verdict on the model that wrote it. The useful half is which of
those lines the generator was still getting wrong after two runs of
patching, which is the next gate.

### The gate, run 2: taste.md against the generator

Runs 1 and 2 answered most of the table above inside
prompts/digest.md. Three lines survived, and this seat had not caught
them because it was grading the issue and not the instrument.

**The date in the title.** Her ruling rescinds the bracketed range.
The prompt's template still opened `# {Editorial title} [{dates}]` and
spent four lines telling the model how to punctuate the brackets. Run 1
rewrote that same title block to make the headline a finding and left
the rescinded suffix standing inside it. The title is the email
subject, so the ruling was being overridden in the one line most
readers ever see.

**Framework names printed.** Covered above.

**Length.** The prompt had no issue-level length rule at all, and two
quotas pulling against her ruling: "3-5 items" on the new-work slot and
"At most five" on the reading list. Depth-follows-significance governed
each item and nothing governed the issue, which is exactly how a thin
day gets padded to look like a rich one.

### What changed in the generator

| Ruling | Change to prompts/digest.md |
| --- | --- |
| Framework names never print (law 12) | The four literal `##` headings become written slots. The fixed-names bullet becomes "Headings are written, not selected": the slots' jobs and order stay house law, their names become internal labels, and each printed heading is written from that day's items, with three worked examples and the test that it must be false for yesterday's issue |
| The same, enforced | A hard pre-output gate in Rules. Read back every line starting with `#`, and if any equals or contains "Gaining traction", "Trailblazing", "Left behind", "Read these yourself", "Compounding", "New and unproven", "Key takeaways" or "What this means", the issue is not finished and the heading is rewritten from the section's items. The title is checked on the same pass |
| Length follows the news (law 11) | A new issue-level rule beside depth-follows-significance, stating that the amount of real material decides, that no section carries a quota, and that padding a thin day and truncating a heavy one are the same failure |
| The same | "3-5 items" becomes as many as the day's new work earns. "At most five" becomes a judged handful, rarely more than five and never five because five was the number. "It is fine for a section to hold 2 items" gains its other half, that it is equally fine for the issue to run long on a day that earned it |
| The date leaves the title | The template's H1 is `# {Editorial title}`. The bracket instructions are replaced by the ruling, and the ASCII rule now applies to a range only where one genuinely belongs in the prose |
| Both, at the reread | The final mechanical-tells pass gains two checks: a heading that would fit any other day, and a length that matches the last issue rather than this day's material |

One seam this created and closed. The headings are brace instructions
now, and the fence above them says to follow the structure exactly, so
a model could have printed the brace. The line introducing the fence
now states that braces are instructions, that they are replaced by the
model's own writing, and that no example sentence quoted from the
prompt survives into the issue. Run 2 found the same class of bug with
the no-meta paragraph sitting unbraced inside the fence, which is why
it was worth looking for.

### The rename is safe, checked again

No Python, JavaScript, template or test in the repo matches on the four
section names. The check is in §4 for the first rename and it still
holds after this one.

Dropping the date from the H1 is also safe, and the frontend already
handles it. `site/lib/content.js:31` parses the title with
`/^(.*?)\s*\[(.+)\]\s*$/` and falls back to `weekRange(week)` when the
brackets are missing, which is the branch the first editions used. So
an issue with a bare title still renders with its date in the archive,
which is where her ruling says the date belongs. The bracket-parsing
branch becomes dead code once no issue carries a range, and retiring it
is the frontend seat's call, filed in the ledger.

### Ban list

Entry 19 amended and three tells appended. The amendment matters as
much as the additions: 19 ended with "The four section names are the
owner's and are fixed", which is the sentence that made this seat's own
ban list an accomplice. It now says the four slots and their order are
hers and their names belong to the skeleton, never to the page.

- **20, the skeleton printed as the page.** The tell itself, with the
  blunt test: a heading that would fit tomorrow's issue is furniture.
- **21, the fixed-length issue.** Ten bullets and nine entries for a
  week whose new work was two papers. Length is a claim about how much
  happened.
- **22, the date inside the headline.** It spends the subject line on a
  fact three other fields already carry.

### Canon

Law 9 listed her four section names as part of her fine-tuning with no
note that they do not print, and law 12 now says they never do. The two
read as a contradiction, and law 9 is the one a future seat would find
first, because it is the law about her authority. Law 9 now names the
four slots by their jobs, keeps her four names as the labels the
generator's author uses, and points at law 12 for what the reader sees.
That is transcription of a ruling recorded in taste.md twice, not a new
law, and the maintenance rule is satisfied. If she reads it otherwise,
the clause comes out.

### Grade for run 3

No new issue, so no new graded issue. The honest grade is on the
generator, and it is the one number worth carrying out of this run:
**three of her sixteen taste lines were still being contradicted by
prompts/digest.md after two runs of patching it, and this seat wrote
two of the three contradictions.** All three are answered above.

The thing to check first on the next graded issue is the headings. Not
whether they are good, which comes second, but whether any of the eight
banned strings appears at all, because that check is now mechanical and
a failure there means the hard gate does not work and the fix has to
move out of the prompt and into the pipeline, per the charter's
structure-watch rule.

---

## 9. Run 4: the prose benchmark, and what the majors do that we do not

Owner's order for this run: produce the prose benchmark against the
major newsletters, presentation-ready for the chair's deck. It is
`docs/voice/prose-benchmark-2026-09-19.md`, and it is the first work in
this repo that reads the competitors' actual sentences rather than
describing them. Everything quoted in it was read live on 2026-09-19
from the issues named in its Sources section.

### The market seat's guide is still a stub, so this run did the study

`docs/market/newsletter-prose-guide.md` is eleven lines and says
"Work in progress". Run 2 flagged that the craft in this branch came
from the canon and the owner's rulings rather than from the actual
newsletters. That gap is now closed by this seat instead, from primary
sources: Morning Brew's Sep 19 issue, TLDR AI's Sep 18 issue, The Batch
issue 371, Import AI #472, and two Money Stuff columns. Money Stuff is
paywalled, so its opening and one section heading are verified and its
sign-off is not, which the benchmark states where it matters.

### The single biggest weakness, run 4

**We are not behind on substance. We are behind on everything the reader
meets before the substance.** Put the five openings side by side and
three of them have a person in the first eight words. Ours announces a
count of themes. The same pattern holds at every other moment of an
issue: four of five majors close on something that is not news, and
2026-W37 closed on a row of pipeline counters.

### What run 4 changed in the generator

Five moves, each traceable to a passage quoted in the benchmark, each
one the majors already make and the prompt did not ask for.

| Move | Seen in | Generator change | Already closed? |
| --- | --- | --- | --- |
| The first-screen promise | Morning Brew's "In today's newsletter, we'll get into:", Money Stuff's deck, The Batch's issue title | The opening gains a fourth job: one line naming the day's two or three items, before any interpreting | No, new tonight |
| Written turns inside a long item | Morning Brew's "From one father to another:", "Zoom out:" | Items past three paragraphs carry short bolded lead-ins written from their own material, with The Batch's fixed beats named as the failure | No, new tonight |
| Saying what the work did not measure | The Batch's "Yes, but:" beat | The in-line evidence grade gains a second half, drawn from `evidence` and `procedure`, never invented | Half. Run 1 made the positive half mandatory |
| The thread back to the older claim | Import AI's "(Import AI #471)", Money Stuff linking six of its own past issues | Items carrying an edge say in a clause what the older belief was and that this updates it, no issue number, no week id | Half. Run 2 offered continuity as one optional opening shape |
| A comparison beside every number | The Batch's "45.9 percent ... versus 37.6 percent without it" | The baseline rule moves from the new-work section to every section | Half. It existed in one section of four |

The final reread pass gained two checks to match: a bare number with no
comparison, and an item past three paragraphs with no written turn.

### One thing I did not do

The benchmark's grades put alexandria's intent at C+ on voice, and I did
not patch the prompt toward a first-person editorial stance, which is
what actually earns Import AI and Money Stuff their A. We ship without a
byline and adding an "I" to an unbylined autonomous newsletter is a
product decision, not a prose one. Filed in the ledger for the owner
rather than decided here.

### Ban list

Three tells appended, all three drawn from the side-by-side comparison
and not from theory: the issue that never says what is in it, the bare
number, and the item that ends on its citation.

### Grade for run 4

Still no new issue, so still no graded issue. The number worth carrying
out of this run is the table in benchmark section 3: on five prose axes
defined from the canon, **2026-W37 scores below every contender on every
axis, including below TLDR AI, which does not try to write at all.** The
patched generator's row is a claim about a prompt and stays provisional
until a model has run it.

## 10. Run 5: the outsider test, and the fix shown on the prose that lost her

The chair hand-ran the rebuilt generator against the real corpus today
and the owner read the result. Her verdict, in full, is the material
this run works from: "much better on prose" AND "it was hard to read in
its entirety and actually understand what's going on. i feel like an
outsider to something privy while reading. thats an issue" AND
"avoiding density is important". Both rulings are now in taste.md and in
the canon as law 12a.

### What I could read, and what I could not

The issue she graded is not in the repo and not in the database. The
`digests` table holds one row, `2026-W37`, written on 2026-09-14, and
nothing since, because the chair ran the generator by hand rather than
through the pipeline. So this run did not read the issue she read, and
nothing below pretends otherwise.

What it did read: her verdict, the generator that produced the issue,
and the claims the issue was built from, pulled live from the database
by paper and by claim id. The two items rewritten further down are
reconstructions from those claims, in the failure mode her verdict
describes, and they are labeled as reconstructions everywhere they
appear. Every number in them is the payload's own.

### The outsider read, now the first pass

Law 12a makes this a first-class axis, ahead of the taste gate and
ahead of the ten laws, and the canon now carries the four-pass order.
The read is simple and it is not a metaphor: one pass, start to finish,
at reading speed, as a builder on another team who has read none of
these papers, grading nothing.

Three questions, three fixes. Where did a sentence have to be read
twice? Where did following along need something nobody handed over?
Would that person have finished it?

She answered all three for us tonight. The third answer was no.

### The single biggest weakness, run 5

**The generator was written to make every sentence good and never to
make the issue readable end to end.** Those are different jobs, and the
prose work of runs 1 through 4 only did the first one. Her two
sentences say so precisely: the prose got better AND the issue got
harder to finish. A reader does not experience an issue as sentences.
They experience it as a distance they either cover or abandon.

Two things make the distance uncoverable, and she named both.

The first is vocabulary. The old rule was one line long and it was
advice: "Plain words for hard concepts. If a term of art is needed,
define it in the same sentence, in a quick clause." Nothing enforced
it, nothing said which words count as terms of art, and a model writing
about distillation does not experience "on-policy distillation" as
jargon at all. It feels like the name of the thing. That is exactly how
a club works, and it is why she used the word "privy".

The second is density. Every rule in the file pushed toward
compression, and compression with no counterweight turns into more
ideas per line rather than fewer words per idea. "Depth follows
significance" tells you how long an item may run. Nothing told you how
much may sit inside one paragraph. So the good sentences stacked up,
four findings and three numbers to a block, and the reader was left to
unpack what should have been handed to them.

### What run 5 changed in the generator

| Move | What was there | What is there now | Law |
| --- | --- | --- | --- |
| The outsider test as a rule | One line of advice about quick clauses | The reader defined as a builder from another team, every term of art carrying its plain-words clause at first use or being replaced by plain words entirely, her "on-policy distillation", "KV cache" and "preference alignment" named as the club words they are, and a sentence that only parses from inside the field marked for rewrite | 12a |
| The first-use pass | Nothing | A hard gate before output: list every term of art, find its FIRST appearance anywhere including the title, the contents line, headings and bold lead-ins, and either the clause goes in that sentence or the term goes. More than about five terms needing a definition means cut an item, not add five clauses | 12a |
| The outsider read | Nothing | A second hard gate: read the whole issue once at reading speed as that builder, with the three questions and their three fixes, and fix what it turns up before the heading gate | 12a |
| The density rule | Nothing at paragraph level | Compression is fewer words per idea and never more ideas per line, one idea per sentence by default, paragraphs of two or three sentences with air between them, and the four-findings-and-a-caveat paragraph named as the failure even when every clause is true | 12a |
| Length running downward | Length follows the news, upward and downward, at issue level | When the day's material would only fit by crowding, cut items and never the air. Fewer findings at a readable pace beat broader coverage in a wall | 11, 12a |
| The item's on-ramp | "Context first, meaning who was stuck on what, in plain words" | The context step is named as the reader's on-ramp and must be in words a builder from another team already knows, naming the problem before the machinery, because an item whose first sentence assumes the vocabulary fails the test in the one place the reader decides whether to keep going | 9, 12a |
| The attribution fallback | "A team led by <first author>", named as the rare fallback | Same rule, plus the fact that it will carry most issues, so vary the construction and never print author names three times in the same shape | 9 |

### The proof, on the exact prose that lost her

Two items, reconstructed from the claims today's issue was built on,
written first in the failure mode and then under the new rules. The
first is the new-work item on RetireOPD. The second is the traction
item on one-example distillation.

#### Item 1, before

> **RetireOPD retires the teacher mid-run**
>
> On-policy distillation has become the default way to lift a small
> agent model toward a larger one's behavior, but the teacher's signal
> pays diminishing returns once the student's policy has converged
> toward it, and RetireOPD adds an Adaptive Retirement criterion that
> tracks the student-teacher discrepancy and drops the teacher once the
> gap stops shrinking and the student reaches a preset fraction of the
> teacher's success rate, after which the run continues on RL alone:
> across Qwen2.5 students from 1.5B to 7B that raises ALFWorld success
> by 14.1% to 18.8% and WebShop accuracy by 11.8% to 19.0% against a
> pure RL baseline, with the skill-free student surpassing its
> skill-conditioned teacher in every setting tested, on the authors' own
> experiments. *RetireOPD: Self-Retiring On-Policy Distillation for
> Agentic Reinforcement Learning*,
> [full text](https://arxiv.org/html/2609.20784)

Nothing in there is false and nothing in there is badly made. It is one
sentence of 103 words carrying nine ideas, and it hands the reader ten
terms it never defines: on-policy distillation, policy, teacher,
student, Adaptive Retirement, RL, ALFWorld, WebShop, skill-free,
skill-conditioned. That is the issue she could not finish.

#### Item 1, after

> **The student got better as soon as it stopped copying the teacher**
>
> Small models that act as agents are usually trained by copying a
> bigger one. The big model shows how a task is done, the small model
> tries it, and its attempts get corrected against the big model's
> behavior. Researchers call this on-policy distillation, meaning the
> corrections land on the small model's own attempts rather than on a
> fixed transcript someone recorded earlier.
>
> The open question is when to stop correcting. Most teams run the
> corrections for the whole training job. A team led by Yan Yu argues
> that you should cut them off early, and that the model gets better
> when you do.
>
> Their method watches the gap between the two models. Once the gap
> stops closing, and once the small model is already hitting a set share
> of the big one's success rate, the big model is dropped. Training
> continues on rewards alone, which means the model simply attempts the
> task and gets scored on whether it finished.
>
> Firing the teacher is what improves it. On ALFWorld, a benchmark of
> household chores written out as text, the runs that dropped the
> teacher finished 14.1 to 18.8 points more tasks than training on
> rewards alone. On WebShop, a simulated shopping site, the same setup
> gained 11.8 to 19.0 points. Both held across Qwen2.5 models from 1.5
> billion to 7 billion parameters, and in every setting the small model
> ended up scoring higher than the model it had been learning from.
>
> Hold that last part loosely. These are the authors' own runs, on two
> benchmarks and one model family, nobody outside the group has
> reproduced them, and nothing here tests a model larger than 7 billion
> parameters.
>
> If you are training a small agent against a bigger one, what to take
> is the stopping rule rather than the method. Measure whether your
> teacher is still adding anything, and stop paying for it when it is
> not.
>
> *RetireOPD: Self-Retiring On-Policy Distillation for Agentic
> Reinforcement Learning*,
> [full text](https://arxiv.org/html/2609.20784)

The after is longer in words and far shorter in work. Same numbers,
same grade, same source. Six short paragraphs instead of one block, one
idea to a sentence, and every term handed over at first use or dropped:
"Adaptive Retirement", "skill-conditioned" and "RL" are gone entirely,
because naming them cost more than they were worth. The headline states
the finding in words nobody needs a field to read.

#### Item 2, before

> **One-shot OPD keeps compounding**
>
> Two more results landed on top of the finding that one-shot OPD,
> trained on a single query, recovers most of full-data OPD's gain
> across task domains and model families, which remains the most useful
> result of the week for anyone running post-training on a budget:
> state-coverage measurements put one query's rollouts at 71.5% of the
> states full-data OPD visits, most of that inside the first 100 steps,
> and 16 semantically diverse queries lift coverage to 98.9% and match
> full-data validation accuracy, so the teacher's signal is distributed
> far more redundantly across the prompt set than the standard recipe
> assumes.

An acronym in the headline, before the prose has unpacked it once. Then
"query", "rollouts", "state coverage", "post-training" and "validation
accuracy" arrive as known quantities inside a single 92-word sentence.

#### Item 2, after

> **One prompt did almost the work of the whole dataset**
>
> Training a small model on a big one's corrections needs practice
> problems to run it on. Collecting those problems and paying the big
> model to answer each one is the expensive half of the job, so everyone
> assumes more of them is better.
>
> Zixuan Fu and colleagues trained on one problem. A single question,
> practiced over and over, kept improving their model for hundreds of
> training steps and recovered most of the gain of using the whole set.
>
> Then they measured why, and the measurement is the useful part. What a
> model learns from is the variety of situations it stumbles into while
> practicing. One question's attempts already reach 71.5% of the
> situations the full dataset reaches, and most of that arrives inside
> the first 100 steps. Sixteen well-spread questions reach 98.9% and
> match the full dataset's accuracy outright.
>
> Treat this as one group's result that has survived its own further
> testing, which is not the same as independent confirmation. The
> follow-up work supporting it so far comes from the same paper.
>
> The experiment this suggests is cheap. Before you budget for sixteen
> thousand practice problems, run sixteen.
>
> *Rethinking On-Policy Distillation of Large Language Models II: One
> Training Example*, [full text](https://arxiv.org/html/2609.04172)

The term "on-policy distillation" never appears in the after outside
the paper's own title, because the plain sentence is better than the
term plus a clause. That is the second half of the rule, and it is the
half a model will resist.

### What the rewrite turned up that no prompt can fix

Writing item 2 honestly meant checking where its support actually came
from, and five of that claim's six `supports` edges are its own paper
supporting itself. Across the whole database, 68 of 98 `supports` edges
join two claims from the same paper. Twenty-four claims currently clear
the traction section's bar of two or more supports. If the bar were two
distinct supporting papers, four would.

That matters more than a normal data bug, because traction is the
owner's standing law and the traction slot leads every issue. The
prompt tells the writer to translate the count into "three separate
papers built on it this week", and for most of today's traction items
that sentence is false. The prompt cannot detect it either, since the
payload carries the count and never the supporting papers' ids. Filed
in the ledger for the engineer with the one-line query fix, per the
charter's structure watch.

The same rewrite turned up a second one. Both papers carry an empty
`institutions` array, and so do 4,718 of the 4,756 papers in the table.
The institution-first law falls through to its author-name fallback
essentially always. Also filed.

### Ban list

Two tells appended, both from her verdict rather than from theory. The
club sentence, which is a term of art standing bare on its first
appearance. The wall, which is an item body written as one block of
stacked clauses.

### Canon

The canon gains a "How an issue is graded" section, making the outsider
read the first of four passes, ahead of the taste gate and the laws,
per law 12a. The laws themselves were not touched, since those move
only on her ruling.

### Grade for run 5

The issue she read: **failing on the axis that decides whether the
product gets read**, on her own verdict, which is the only grade that
counts here and is more reliable than mine would have been. The prose
improvements of runs 1 through 4 hold. They were aimed one level too
low.

The generator after tonight: unproven, as always until a model runs it.
The honest risk is that "define every term at first use" produces an
issue clotted with definitions, which is the density failure wearing
the outsider fix's clothes. Two things guard against that. The rule
says to drop the term and write plain words when the definition costs
more than the term is worth, and the first-use pass treats more than
about five needed definitions as a signal to cut an item. Whether the
guard holds is the first thing to check on the next issue.
---

## 10. Run 5: the second cold read, and the labels that moved down a level

Fifth run the same day, scheduled dispatch with no owner instructions.
The digests table still holds exactly one row, **2026-W37**, written
2026-09-14 against prompt `83a0aa3be13c`. No issue has been generated
since run 1 graded it, so there is still no output from the patched
generator to judge, and run 4's closing sentence stands: the patched
prompt's quality is a claim about a prompt until a model has run it.

What this run could do instead was read 2026-W37 cold a second time,
from the database and before opening this file, and then check every
tell it found against what runs 1 to 4 already caught. Four survived
that check. All four are still reachable through the current prompt,
and three of them are reachable because the prompt hands the model the
mould itself.

### The single biggest weakness, run 5

**The generator now forbids the reader-facing template everywhere
except the three places where it supplies one.** Run 3 and run 4 built
a real gate against printed framework names, down to a literal string
check before output. Two hundred lines below that gate, the
fell-behind section told the model to write `**Contradicted**` and
`**Replaced**` and handed it the exact lead-in sentence to use. The
model obeyed, verbatim:

> **Contradicted** – First, the claims newer evidence says are wrong:

That line is the prompt's own example, printed word for word, under a
rule that says no sentence quoted from this file survives into the
issue. A category word is a category word whether it carries a `##` or
a pair of asterisks, and "Contradicted" would fit every issue this
newsletter will ever publish. The same shape appears twice more: the
item spine ends every item on a consequence, and 2026-W37 wrote that
consequence three times out of three as "Builders should" or
"Practitioners should". The reading list's one-line reasons opened six
times out of nine on a verb of presentation, "shows", "details",
"offers", "introduces", "presents", "proposes". A rule that mandates a
part without varying its construction produces a column, and a column
is a template with prose in it.

### The cold read, against what runs 1 to 4 caught

Everything else this read found was already on the board. The printed
framework headings, the bracketed date, the masthead, the missing
close, the abstract links, the em dashes, the buried traction section,
"(3 supports)", one paper across three slots, nine reading-list
entries, the non-breaking hyphens and the self-defending section intro
are ban list entries 13 to 25 and §1 to §9 above. That agreement is
worth stating: a second cold read of the same issue produced the same
verdicts, which is the closest thing this seat has to a calibration
check on its own instrument.

Four tells were new.

| Tell | Evidence from 2026-W37 | Why the current prompt still allows it |
| --- | --- | --- |
| The label one level down | "**Contradicted** – First, the claims newer evidence says are wrong:" | Line 113 explicitly exempted the fell-behind lead-ins from the heading law, and lines 354 and 363 supplied both the labels and the sentences |
| The advice sentence in one construction | "Builders should replace...", "Practitioners should adopt...", "Builders of long-horizon agents should embed..." | The spine bans "the same sentence shapes" in the abstract, but the reread gate, which is the part a 120B model actually executes, never named it |
| The forecast nothing can check | "<u>This shift suggests that future pipelines will embed verification and observation cues as core components rather than add-on tricks.</u>" | No rule covered it. The hedging ban names "it seems that" and "arguably", and this sentence hedges nothing. It simply cannot be wrong |
| The invented shared byline | "Finally, researchers at the same group released a 122B Mixture-of-Experts terminal agent (T1)" | The never-invent rule covers "papers, claims, numbers, or links". Attribution was not in the list, and the payload never said the two papers shared a team |

The fourth is the one that matters most, because it is not a prose
defect. The issue told a paying reader that two unrelated papers came
from one lab. Every other tell in this file makes the writing worse.
That one makes it false.

### What run 5 changed in the generator

| Change | Serves | What it prevents |
| --- | --- | --- |
| The fell-behind section's two kinds lose their bold labels and their supplied lead-ins, and are introduced by a written sentence built from that day's items | Canon law 12, ban list 30 (numbered 26 when this line was written, see the ban list header) | The generator printing its own skeleton one level below the heading gate |
| The pre-output gate reads back bold runs at the start of a line, not only `#` lines | Canon law 12 | The same violation slipping past a gate that only looked at headings |
| The spine's consequence part gains a variation rule that changes what the consequence attaches to, not only its verb | Ban list 3, 12, 27 | Three items running that end "Builders should X" |
| A new voice rule: no sentence about the future that no result can check, and emphasis goes on results, never predictions | Canon laws 2 and 6, ban list 8, 28 | The direction-of-travel sentence, and the issue's one underline spent on it |
| Attribution carries the never-invent rule explicitly, and a shared-team claim requires the payload to show the overlap | Canon law 9, ban list 29 | Telling the reader two papers came from one group when they did not |
| The reading list's reasons may not open on a verb of presentation | Ban list 3 | Six entries out of nine in one column |
| Three checks added to the final reread: two items closing in the same shape, a prediction with no number or date, a bold word standing in for a heading | Canon law 12 | All of the above, at the only moment the model is told to look back |

### Ban list

Four tells appended, 26 to 29, all four from lines in 2026-W37 that
runs 1 to 4 did not quote: the label that moved down a level, the
advice sentence in the same clothes every time, the forecast nothing
can check, and the invented shared byline.

### Structure watch

The charter's rule is that a structural fix failing twice through the
prompt goes to the engineer. The heading fix is at one prompt attempt
with a hard gate, pre-registered in the ledger under run 3, and
nothing is owed until an issue breaks it.

One thing filed fresh: **the ASCII punctuation rule does not belong in
a prompt at all.** It is now a paragraph of instruction plus a line in
the reread gate, asking a 120B model to police its own keystrokes, for
the one defect class that needs no judgment. U+2011 has exactly one
correct replacement. Filed for the engineer as a silent normalization
between generation and the insert, deliberately not a failing gate,
because failing an issue over a hyphen costs more than substituting
one.

### Grade for run 5

Still no issue written by the patched generator, so still no grade
against the ten laws. The number this run produces is a different one
and it is about this seat rather than the product: **a second
independent cold read of 2026-W37 found four defects that four prior
runs missed, and three of the four trace to the prompt supplying a
template rather than failing to forbid one.** The lesson generalizes
past these four lines. Every rule this seat adds that mandates a part
of an item, a lead-in, a closing consequence, a reason line, has to
supply the variation along with the mandate, or it will be obeyed into
a column. Incident 20 said recording a ruling is not enforcing it.
This run's version is narrower and sharper: mandating a part is not
the same as writing one.

---

## 12. The merge pass: what the two run 5s did to each other

Owner dispatch. Section 10 and section 11 were written the same day
against the same file, section 10 landed on main first as #48, and this
branch's four findings had to be re-applied on top of the merged
generator rather than rebased over it or reverted out of it. Four files
conflicted. What follows is the part worth keeping: where the two runs
disagreed, and what reading the merged prompt end to end turned up that
neither run could have seen alone.

### The one passage where both runs were right

Attribution. Section 10 found that `institutions` is empty on 99.2% of
papers, so "a team led by X" will carry most items, and told the writer
to vary the construction. Section 11 found that 2026-W37 had invented a
shared byline, "researchers at the same group", tying two unrelated
papers to one lab the payload never described.

Those two instructions pull against each other, and that is the
interesting part. Telling a writer to vary a construction is telling it
to reach for another phrase, and "the same team" is sitting right there
looking like a variation. The merged rule keeps both and names the join:
vary the construction, and varying the construction is not licence to
vary the fact. Where the payload does not show the overlap, two entries
name two different sets of people.

### The two specimens, and whether the gate catches them

The owner added two rejected intro specimens to taste.md with this
dispatch, and asked whether the merged first-use gate would catch them.
Read plainly, it catches one.

**"Gradients": yes.** The pass tells the writer to list "training
vocabulary", and gradients are training vocabulary by any reading. It
would be listed, its first appearance found, and the sentence
"preference alignment can work without gradients at all" would fail on
two terms at once.

**"Teacher" and "student": no, and the reason is the useful part.** The
pass lists what to hunt for: method names, training vocabulary, metric
and benchmark names, coined names, acronyms. A writer walking the issue
for terms of art does not list "teacher". It is an English word, it
appears to be doing ordinary work in the sentence, and nothing on the
page marks it. The gate has nothing to fail on, so the sentence ships.
That is the whole failure: not that the rule was too weak, but that the
class was not on the list.

The merged file was also modelling it. Section 10 wrote the item spine's
on-ramp example, the one sentence the file offers as the plainest a
context step can be written in, as "models that learn from their own
attempts keep drifting away from the teacher that was supposed to
correct them". The prompt's exemplar of the outsider fix was itself the
specimen she rejected. That is section 11's own lesson arriving from the
other direction: the prompt hands the model the mould, and this time the
mould was inside the rule meant to prevent it.

Fixed, three ways. The borrowed ordinary word is now its own named class
in the first-use pass, called out as the class that slips through
because it does not look like jargon. "Gradients" joins the club words
by name, with her repaired sentence beside it. The spine example says
"the bigger model", and the contents-line example loses a bare "credit
assignment" for the same reason. Ban list 32 records the tell.

### The other seams

Three more, found by reading the merged file top to bottom rather than
by diffing it.

| Seam | Where it came from | Fix |
| --- | --- | --- |
| The opening was not named as the strictest place for the first-use rule, though both specimens came from one | Neither run wrote the taste entry; it arrived with this dispatch | The invariant paragraph now says the opening carries the strictest form of the rule, because that is where an outsider decides whether the issue is for them |
| The outsider read's own fixes run AFTER the first-use pass, and both reach backwards | Section 10 ordered the gates and did not close the loop | A term handed over during the outsider read goes in at its first appearance, not where it was noticed, and cutting an item can strand a definition the rest of the issue leaned on |
| The forecast ban could be read as gutting the sign-off's "what is still unsettled" | Section 11's rule, three hundred lines above section 10's untouched sign-off block | The ban now says outright that naming an open question is not forecasting; the prohibition is on predicting the answer |

### Grade for the merge pass

No new issue, so still no grade against the ten laws, and that sentence
has now closed five runs in a row. What this pass produces instead is a
count: **the two run 5s touched the same passage once and produced a
contradiction, and the merged file contained one instance of the exact
defect its own new rule exists to prevent.** Neither run could have seen
that in its own diff. Both were visible in ten minutes of reading the
whole file in order, which is the same move the outsider read asks of
the generator, applied to the generator.

The standing risk is unchanged and now has a second head. Section 10
named it: "define every term at first use" may clot an issue with
definitions. This pass adds a class of terms to define, so the guard
matters more, not less. Both guards still stand, the clause that says drop the term and write
the plain thing, and the five-definition ceiling, and the first issue
out of this file is where they get tested.
